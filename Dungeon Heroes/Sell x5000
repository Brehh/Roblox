

-- This code was generated by Cobalt
-- https://github.com/notpoiu/cobalt

local Event = game:GetService("ReplicatedStorage").Systems.Combat.PlayerSkillAttack
local mtHook; mtHook = hookmetamethod(game, "__namecall", function(self, ...)
	local Args = table.pack(...)
	if getnamecallmethod() == "FireServer" then
		if self.Name == "SellItem" then
			local newargs = {}
			for i,v in next,Args[1] do
				for x= 1,5000 do
					table.insert(newargs, v)
				end
			end
			Args[1] = newargs
			return mtHook(self, table.unpack(Args, 1, Args.n))
		end
	end

	return mtHook(self, ...)
end)
