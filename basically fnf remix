local wow = game:GetService("ReplicatedStorage").Remotes.DataF:InvokeServer()
local curkey = wow[1].Inputs
local RunService = game:GetService('RunService')

local UserInputService = game:GetService("UserInputService")
local ENUM_ALIASES = {}

-- 1. Grab every single Enum category in Roblox (KeyCode, Material, etc.)
for _, enumItem in ipairs(Enum.KeyCode:GetEnumItems()) do
    local name = string.upper(enumItem.Name)
    
    -- 2. ใช้ฟังก์ชันของ Client แปลง KeyCode ให้กลายเป็น "ตัวอักษรพิมพ์" จริงๆ 
    -- เช่น Enum.KeyCode.A -> "a", Enum.KeyCode.One -> "1"
    local success, character = pcall(function()
        return UserInputService:GetStringForKeyCode(enumItem)
    end)
    
    if success and character and character ~= "" then
        -- แปลงตัวอักษรที่ได้ (มักจะเป็นพิมพ์เล็ก) ให้เป็นรหัส UTF-8 ของตัวพิมพ์ใหญ่
        local upperChar = string.upper(character)
        ENUM_ALIASES[name] = utf8.codepoint(upperChar)
    end
end

-- Custom fallback function from before
local function getByteValue(input)
    -- Look up the uppercase version of the string in our automatic map
    local lookupKey = string.upper(input)
    if ENUM_ALIASES[lookupKey] then
        return ENUM_ALIASES[lookupKey]
    end
    
    -- Fallback to standard dynamic UTF-8 character conversion
    local success, codePoint = pcall(utf8.codepoint, input)
    return success and codePoint or nil
end

local function pressKey(key)
	local truekey = getByteValue(key)
    if truekey then keypress(truekey) end
end

local function releaseKey(key)
	local truekey = getByteValue(key)
    if truekey then keyrelease(truekey) end
end

if _G.bruh then
    _G.bruh:Disconnect()
    _G.bruh = nil
end

local plr = game.Players.LocalPlayer
local gui = plr.PlayerGui
local file = plr.File
local ready = file.Ready
local curside = file.CurrentPlayer
_G.on = true

local curready
local savedKey = {}
local activeNotes = {} -- Table สำหรับเก็บสถานะ Note แทนการสร้าง task.spawn

if _G.on then
    print("WORK (Optimized Version)")
    
    -- Cache ฟังก์ชันคณิตศาสตร์และตัวหนังสือเพื่อความเร็ว
    local math_abs = math.abs
    local tonumber = tonumber
    local string_match = string.match
    
    _G.bruh = RunService.RenderStepped:Connect(function()
        if not ready.Value then
            if curready then
                curready = false
                -- ปล่อยปุ่มทั้งหมดที่ค้างอยู่เมื่อจบเพลง
                for _, data in pairs(activeNotes) do
                    if data.pressed then
                        releaseKey(data.key)
                    end
                end
                savedKey = {}
                activeNotes = {}
				print("Note Ready")
            end
            return
        elseif not curready then
            curready = true
			local wowxd = game:GetService("ReplicatedStorage").Remotes.DataF:InvokeServer()
			curkey = wowxd[1].Inputs
			print("Ready Now!")
        end

        local side = string_match(curside.Value.Name, "%d+")
        local main = gui.Main.MatchFrame
        local myside = main["KeySync" .. side]

        -- 1. ค้นหาและลงทะเบียน Note ใหม่
        for _, v in ipairs(myside:GetChildren()) do
            if string.find(v.Name, "Arrow") then
                local keyIndex = tonumber(string_match(v.Name, "%d+"))
                local targetKey = curkey[keyIndex]

                for _, note in ipairs(v.Notes:GetChildren()) do
                    if note.ClassName == 'ImageLabel' and not savedKey[note] then
                        savedKey[note] = true
                        
                        -- ค้นหา Hold note แค่ "ครั้งเดียว" ตอนพบ Note แทนที่จะหาทุกเฟรม
                        local holdNote = note.Parent:FindFirstChild("Hold_"..note.Name)
                        
                        -- บันทึกลงในระบบ
                        activeNotes[note] = {
                            key = targetKey,
                            hold = holdNote,
                            pressed = false
                        }
                    end
                end
            end
        end

        -- 2. ตรวจสอบและกด Note ทั้งหมดในลูปเดียว
        for note, data in pairs(activeNotes) do
            -- ถ้า Note ถูกเกมทำลายไปแล้ว ให้ปล่อยปุ่มและลบออกจากระบบ
            if (not note.Parent) and ((not data.hold) or (not data.hold.Parent)) then
                if data.pressed then releaseKey(data.key) end
                activeNotes[note] = nil
                continue
            end

            -- จัดการจังหวะกด
            if not data.pressed then
                if note.Position.Y.Scale >= -0.05 then
                    pressKey(data.key)
                    data.pressed = true
                    
                    -- ถ้าไม่ใช่ปุ่ม Hold ให้ปล่อยทันที (จำลองการทำงานเดิม)
                    if not data.hold then
                        releaseKey(data.key)
                        activeNotes[note] = nil
                    end
                end
            -- จัดการจังหวะปล่อย (สำหรับปุ่ม Hold)
            else
                local hold = data.hold
                if hold then
                    if hold.Position.Y.Scale >= math_abs(hold.Size.Y.Scale) then
                        releaseKey(data.key)
                        activeNotes[note] = nil
                    end
                end
            end
        end
    end)
end
